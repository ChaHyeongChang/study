
#include <iostream>

int main(){
    int sum;
    for(int i = 1; i <= 10; i++){
        sum += i;
    }

    //std::cout을 이용해서 숫자, 문자, 문자열 등을 출력할 수 있음!
    std::cout << "1에서 10까지 더한 결과는 " << sum << "입니다\n";

    char c = '#';
    std::cout << c << 5.5 << '-' << "Hi"; //변수, 숫자, 문자, 문자열을 출력함

    //하나하나마다 줄바꿈을 하고 싶으면
    /*
        //std::endl은 줄바꿈(\n)을 추가하고 !!출력 버퍼를 비우는 기능!!도 포함한다.

        std::cout << c << std::endl;
        std::cout << 5.5 << std::endl;
        std::cout << '-' << std::endl;
        std::cout << "Hi" << std::endl;
    */
    /*
        //일반적으로 사용(\n) --> '\n'을 사용하면 출력 버퍼가 자동으로 비워지지 않아서 성능면에서 더 유리할 수도 있음

            ->출력 버퍼를 비우는 것은 운영체제(OS)와의 입출력(IO) 작업이 필요, 이 작업은 비싼 연산(비효율적 연산)
        
            -->'\n'을 사용하면 줄바꿈만 수행하고 버퍼를 유지하므로 여러 개의 데이터를 버퍼에 모아두었다가 한 번에 출력하여 성능 향상!

            ---> 하지만 즉시 출력이 필요한 경우(사용자로부터 입력받기, 로그를 실시간으로 확인)에는 std::endl을 사용해야 한다다
        std::cout << c << '\n';
        std::cout << 5.5 << '\n';
        std::cout << '-' << '\n';
        std::cout << "Hi" << '\n';
    */
    /*
        //냅다 한줄로 박기

        std::cout << c << '\n' << 5.5 << '\n' << '-' << '\n' << "Hi" << '\n';
    */    


    return 0;
}
