
1. int main(void) 
  의미: void는 이 함수가 매개변수를 받지 않는다는 것을 명시적으로 나타냅니다.
  특징:
        - void를 사용하면 컴파일러가 함수가 인수를 받을 수 없도록 제한합니다.
        - 더욱 엄격하고 명확하게 함수의 의도를 전달합니다.
        - 특히 함수의 매개변수와 관련된 오타나 실수를 방지할 수 있습니다.

  권장 상황: 매개변수가 없는 함수임을 명확히 나타내고 싶을 때 사용합니다.

/*
#include <stdio.h>
int main(void) {
    printf("Hello, World!\n");
    return 0;
}
*/



2. int main()
  의미: ()는 매개변수의 유무를 제한하지 않는다는 의미로 해석됩니다.
  특징:
        - C 언어에서는 int main()을 사용할 경우, 컴파일러는 이 함수가 임의의 개수와 타입의 매개변수를 받을 수도 있다고 간주합니다.
        - 이는 C++에서는 int main()이 int main(void)와 동일하게 처리되는 점과 차이가 있습니다.

  주의점: 과거 C 표준(C89)에서는 허용되었지만, C99 및 이후 표준에서는 void를 명시적으로 쓰는 것이 더 권장됩니다.

/*
#include <stdio.h>
int main() {
    printf("Hello, World!\n");
    return 0;
}
*/
