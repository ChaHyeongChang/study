p219 a4용지 필기
//이중 포인터 : 어떤 변수의 주소를 저장하려면 그 변수보다 *가 무조건 1개 더 많아야 한다. ex)계급장

#include <stdio.h>

int main(void){
    int a = 5;
    int *p = &a;

    //p가 포인터의 주소이므로 포인터를 2개 선언해야함
    int **p2 = &p; // p2는 p의 주소를 저장하는 포인터 변수
    

    //p2가 더블 포인터의 주소이므로 포인터를 3개 선언해야함함
    int ***p3 = &p2; // p3는 p2의 주소를 저장하는 포인터 변수

    //a : 5
    //p : a의 주소
    //*p : 5(역참조 1번번)

    //p2 : p의 주소
    //*p2 : a의 주소(역참조 1번)
    //**p2 : 5(역참조 2번)

    //p3 : p2의 주소
    //*p3 : p의 주소(역참조 1번)
    //**p3 : a의 주소(역참조 2번)
    //***p3 : 5 (역참조 3번)

    return 0;
}
