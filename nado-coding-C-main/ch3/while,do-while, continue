
#include <stdio.h>

int main(void){
    
    /* int i = 0;

    while (1){

        printf("S1x umai%d\n", i++);

        if(i == 5){
            break;  // i 가 5면 나가잇잇
        }

    }

    */

//do while 문은 일단 무조건 한 번은 실행하고 난 뒤에 조건을 확인해서 조건이 참이면 실행하고 조건이 거짓이면 반복문을 빠져나온다.

    /**
    int i = 0;

    do {
        printf("asdfjlaskfj;%d\n", i++); 
    } while (i < 5); 
    */
    /*
    for (int i = 0; i < 5; i++){

        if(i == 3){
            continue;  // 3일때 컨티뉴 이므로 아래 printf를 실행하지 않고 바로 조건으로 넘어감감
        }
        printf("가나다라마사%d\n", i);
    }
    */

    /*
    for (int i = 0; i < 5; i++){

        printf("가나다라마사%d\n", i);

        if(i == 3){
            continue; //어짜피 위에서 가다라마사3이 적힌 다음에 컨티뉴 되는거라 01234 전부 출력됨됨
        }
    }
    */

    /*
    int i = 0;

    while (i < 5){
        printf("아아앙%d", i++);

        if (i == 3){
            break;
        }

        printf("이건 못참쥥\n");
    }
    */

    int i = 0;

    while (i < 5){
        printf("asdfjlaskfj;%d\n", i++);
    }

    return 0;
}
